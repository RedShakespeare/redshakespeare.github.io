name: Build Hexo and Deploy to main

on:
  push:
    branches: [ "src" ]
  workflow_dispatch:
    inputs:
      sync_mode:
        description: "R2 asset sync mode"
        required: true
        default: incremental
        type: choice
        options:
          - incremental
          - full

permissions:
  contents: write

concurrency:
  group: hexo-deploy
  # Legacy R2 mirror runs must finish; cancelling one can leave objects missing.
  cancel-in-progress: false

jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout src
        uses: actions/checkout@v4
        with:
          ref: src
          fetch-depth: 0
          lfs: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Install video browser test engines
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Determine asset pipeline mode
        id: asset-pipeline
        run: npm run assets:mode

      - name: Hydrate Pages LFS asset
        if: steps.asset-pipeline.outputs.mode == 'legacy'
        run: git lfs pull --include="source/img/df.zip"

      - name: Refresh legacy asset manifest
        if: steps.asset-pipeline.outputs.mode == 'legacy'
        run: npm run assets:seed

      - name: Run podcast preflight
        run: |
          npm run test:assets
          npm run test:hexo-sil-audio
          npm run test:hexo-sil-archive
          npm run test:hexo-sil-video
          npm run test:hexo-sil-video:browser
          npm run test:hexo-sil-podcast
          npm run test:hexo-sil-podcast-inside
          npm run test:podcast-feed-verifier
          npm run test:r2-worker
          npx hexo clean
          npx hexo generate --bail

  sync-assets:
    needs: preflight
    runs-on: ubuntu-latest
    steps:
      - name: Checkout src
        uses: actions/checkout@v4
        with:
          ref: src
          fetch-depth: 0
          lfs: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci || npm i

      - name: Determine asset pipeline mode
        id: asset-pipeline
        run: npm run assets:mode

      - name: Detect R2 asset changes
        id: assets
        if: steps.asset-pipeline.outputs.mode == 'legacy'
        run: npm run assets:legacy-sync -- --detect
        env:
          SYNC_MODE: ${{ inputs.sync_mode || 'incremental' }}
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
          GITHUB_SHA: ${{ github.sha }}

      - name: Install R2 sync tools
        if: steps.asset-pipeline.outputs.mode == 'legacy' && steps.assets.outputs.mode != 'none'
        run: |
          sudo apt-get update
          sudo apt-get install --yes git-lfs rclone

      - name: Sync assets to R2
        if: steps.asset-pipeline.outputs.mode == 'legacy' && steps.assets.outputs.mode != 'none'
        run: npm run assets:legacy-sync -- --sync --mode "${{ steps.assets.outputs.mode }}"
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          RCLONE_CONFIG_R2_TYPE: s3
          RCLONE_CONFIG_R2_PROVIDER: Cloudflare
          RCLONE_CONFIG_R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          RCLONE_CONFIG_R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          RCLONE_CONFIG_R2_ENDPOINT: https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com
          RCLONE_CONFIG_R2_REGION: auto
          RCLONE_CONFIG_R2_FORCE_PATH_STYLE: "true"
          RCLONE_CONFIG_R2_NO_CHECK_BUCKET: "true"
          RCLONE_CONFIG_R2_NO_HEAD: "true"
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
          GITHUB_SHA: ${{ github.sha }}

  build:
    needs: [preflight, sync-assets]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout src
        uses: actions/checkout@v4
        with:
          ref: src
          fetch-depth: 0
          lfs: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci || npm i

      - name: Determine asset pipeline mode
        id: asset-pipeline
        run: npm run assets:mode

      - name: Hydrate Pages LFS asset
        if: steps.asset-pipeline.outputs.mode == 'legacy'
        run: |
          git lfs pull --include="source/img/df.zip"

      - name: Refresh legacy asset manifest
        if: steps.asset-pipeline.outputs.mode == 'legacy'
        run: npm run assets:seed

      - name: Generate
        run: |
          npx hexo clean
          npx hexo generate --bail
          if [ "${ASSET_PIPELINE_MODE}" = "r2" ]; then
            rm -rf public/files
          fi
          # 可选：避免 Jekyll 处理导致下划线目录被忽略
          touch public/.nojekyll
        env:
          ASSET_PIPELINE_MODE: ${{ steps.asset-pipeline.outputs.mode }}

      - name: Verify generated asset policy
        run: |
          if [ "${ASSET_PIPELINE_MODE}" = "r2" ] && [ -e public/files ]; then
            echo "public/files must be served by R2 after the manifest is finalized"
            exit 1
          fi
          if [ "${ASSET_PIPELINE_MODE}" = "r2" ] && rg -l --glob '*' 'version https://git-lfs.github.com/spec/v1' public; then
            echo "Generated site contains Git LFS pointer files"
            exit 1
          fi
        env:
          ASSET_PIPELINE_MODE: ${{ steps.asset-pipeline.outputs.mode }}

      - name: Deploy public/ to main root
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: main
          folder: public
          clean: true
          commit-message: "deploy: ${{ github.sha }}"
